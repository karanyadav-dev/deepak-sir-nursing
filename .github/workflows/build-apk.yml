name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  # P4A stable version
  BUILDOZER_WARN_ON_ROOT: 0

jobs:
  build:
    runs-on: ubuntu-22.04
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      
      - name: Setup Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential git zip unzip \
            openjdk-17-jdk autoconf libtool \
            pkg-config zlib1g-dev libncurses-dev \
            cmake libffi-dev libssl-dev ccache \
            libltdl-dev automake
      
      - name: Install Python Dependencies
        run: |
          pip install --upgrade pip
          pip install buildozer==1.5.0 cython==0.29.33
      
      - name: Download and Setup Android SDK
        run: |
          mkdir -p $HOME/android-sdk/cmdline-tools
          cd $HOME/android-sdk
          
          # Download command line tools
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip
          
          # Extract into temp folder
          unzip -q commandlinetools-linux-11076708_latest.zip -d cmdline-tools-temp
          
          # ZIP structure: cmdline-tools/cmdline-tools
          mv cmdline-tools-temp/cmdline-tools $HOME/android-sdk/cmdline-tools/latest
          
          # Cleanup
          rm -rf cmdline-tools-temp
          rm -f commandlinetools-linux-11076708_latest.zip
          
          # Make executable
          chmod +x $HOME/android-sdk/cmdline-tools/latest/bin/*
          
          # Verify
          ls -la $HOME/android-sdk/cmdline-tools/latest/bin/
      
      - name: Setup Android SDK & Licenses
        run: |
          export ANDROID_HOME=$HOME/android-sdk
          export ANDROID_SDK_ROOT=$HOME/android-sdk
          export PATH=$PATH:$HOME/android-sdk/cmdline-tools/latest/bin
          
          # Accept licenses
          yes | sdkmanager --licenses --sdk_root=$ANDROID_HOME || true
          
          # Install packages
          sdkmanager "platform-tools" --sdk_root=$ANDROID_HOME || true
          sdkmanager "platforms;android-33" --sdk_root=$ANDROID_HOME || true
          sdkmanager "build-tools;33.0.2" --sdk_root=$ANDROID_HOME || true
          
          # Setup buildozer path
          mkdir -p $HOME/.buildozer/android/platform
          ln -sf $HOME/android-sdk $HOME/.buildozer/android/platform/android-sdk
          
          # Create tools/bin symlink (buildozer expects this)
          mkdir -p $HOME/android-sdk/tools/bin
          ln -sf $HOME/android-sdk/cmdline-tools/latest/bin/sdkmanager $HOME/android-sdk/tools/bin/sdkmanager
          ln -sf $HOME/android-sdk/cmdline-tools/latest/bin/avdmanager $HOME/android-sdk/tools/bin/avdmanager
          
          # Verify
          ls -la $ANDROID_HOME/
          echo "--- Build tools ---"
          ls -la $ANDROID_HOME/build-tools/ || echo "no build-tools"
      
      - name: Create buildozer.spec (Fixed)
        run: |
          cd frontend
          cat > buildozer.spec << 'EOF'
          [app]
          title = Deepak Sir Nursing
          package.name = deepaksir
          package.domain = com.deepaksir
          source.dir = .
          source.include_exts = py,png,jpg,kv,atlas,ttf,json
          version = 1.0.0
          requirements = python3,kivy==2.2.1,kivymd==1.1.1,cython==0.29.33,requests,urllib3,chardet,idna,certifi,plyer
          orientation = portrait
          fullscreen = 0
          
          [android]
          permissions = INTERNET,ACCESS_NETWORK_STATE,CAMERA
          android.api = 30
          android.minapi = 21
          android.ndk = 25b
          android.arch = arm64-v8a
          android.entrypoint = org.kivy.android.PythonActivity
          android.private_storage = True
          android.accept_sdk_license = True
          android.bootstrap = sdl2
          android.sdk_path = /home/runner/.buildozer/android/platform/android-sdk
          android.ndk_path = /home/runner/.buildozer/android/platform
          p4a.branch = v2023.09.30
          
          [python]
          source.include_py = main.py
          
          [buildozer]
          log_level = 2
          warn_on_root = 0
          EOF
          cat buildozer.spec
      
      - name: Build APK
        id: build
        continue-on-error: true
        env:
          ANDROID_HOME: /home/runner/android-sdk
          ANDROID_SDK_ROOT: /home/runner/android-sdk
          BUILDOZER_WARN_ON_ROOT: 0
        run: |
          cd frontend
          export PATH=$PATH:$HOME/android-sdk/cmdline-tools/latest/bin:$HOME/android-sdk/tools/bin
          buildozer -v android debug 2>&1 | tee build.log
          echo "BUILD_EXIT=$?" >> $GITHUB_OUTPUT
      
      - name: Check Build Output
        if: always()
        run: |
          echo "=== Build Exit Code ==="
          echo "${{ steps.build.outputs.BUILD_EXIT }}"
          echo ""
          echo "=== APK Search ==="
          find /home/runner -name "*.apk" 2>/dev/null || echo "❌ No APK found"
          echo ""
          echo "=== bin folder ==="
          ls -la frontend/bin/ 2>/dev/null || echo "❌ No bin folder"
          echo ""
          echo "=== Last 100 lines of build log ==="
          tail -100 frontend/build.log 2>/dev/null || echo "❌ No log"
      
      - name: Upload APK
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: deepaksir-apk
          path: |
            frontend/bin/*.apk
            **/*.apk
          if-no-files-found: warn
      
      - name: Upload Build Log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: frontend/build.log
          if-no-files-found: warn